---
phase: 08-venomscore-and-august-15-demo-execution
plan: 04
type: execute
wave: 3
depends_on:
  - 08-02-PLAN.md
  - 08-03-PLAN.md
files_modified:
  - backend/seed_demo.py
  - backend/tests/test_seed_demo.py
  - backend/tests/test_routes.py
autonomous: true
requirements:
  - DEMO-DATA-01
  - E2E-TEST-01
reference: "docs/AUGUST_15_EXECUTION_PLAN.md#hour-5--demo-seed-data-person-b-while-person-a-polishes-ui"
estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "backend/seed_demo.py runs standalone (`cd backend && python seed_demo.py`) and exits 0 — sys.path.insert(0, ROOT/backend) makes `from app import database as db` work (setup.py:99 contract); NAGRAKSHA_DB defaults to db/nagraksha.db"
    - "Seed is idempotent: hospitals upserted by name, AntivenomStock delete-then-insert per demo hospital, VillageAudit upserted by (gramPanchayat, district) — a second run produces identical row counts (test-asserted)"
    - "Five Karnataka hospitals seeded with compliance: Mandya District Hospital 91.5, Tumkur District Hospital 78.0, Hassan District Hospital 56.0, K.R. Hospital Mysore 88.0, Rajarajeshwari Medical Nagara 45.0; stock status mapped CONFIRMED (>70) / LOW (>45) / OUT"
    - "Three stakeholders seeded (Gerry Martin/The Liana Trust, Dr. Ravi Shankar, NSS Coordinator) all with supportType 'pilot_permission' — transparent demo marking, not verified-consent claims"
    - "Three VillageAudit rows seeded: Malavalli/Mandya risk 58.0, Srirangapatna/Mandya 72.0, Tiptur/Tumkur 41.0 with ashaWorkerId 'asha-worker-001' and auditDate 2026-08-10"
    - "backend/tests/test_seed_demo.py asserts exact names, compliance values, stock mapping, aggregates, and re-run idempotency against a temp NAGRAKSHA_DB"
    - "Two-session loop test (TestVenomScoreHospitalLoop) proves victim→backend→hospital: two broadcast events named VENOM_SCORE_UPDATE with venomType progression UNKNOWN → NEUROTOXIC"
    - "Full gate green: backend pytest (all), frontend vitest, frontend lint 0 errors, frontend build 0 errors; manual two-browser rehearsal checklist executed (human check)"
  artifacts:
    - path: "backend/seed_demo.py"
      provides: "Idempotent Karnataka demo seed script"
      contains: "Mandya District Hospital, Gerry Martin, Malavalli, pilot_permission"
    - path: "backend/tests/test_seed_demo.py"
      provides: "Post-seed correctness + idempotency assertions"
      contains: "assert" blocks for hospitals/stakeholders/audits counts and values
    - path: "backend/tests/test_routes.py"
      provides: "TestVenomScoreHospitalLoop two-session real-time test"
      contains: "VENOM_SCORE_UPDATE"
  key_links:
    - "seed_demo.py upsert pattern <-> app/seed.py:28-52 (name-based upsert; Hospital.name is not unique so bare INSERT OR IGNORE duplicates rows)"
    - "seed compliance columns <-> database.migrate_db() ALTERs (init_db() must run before compliance inserts)"
    - "hospital loop test <-> venom_score.py broadcast + domain compute (real-time path, not just unit)"
---

# Plan 08-04: Demo Seed Data & Integration Rehearsal

## Objective

Create and execute an idempotent Karnataka demo seed script and prove the full victim→backend→hospital real-time loop with an automated two-session test plus a rehearsable manual demo run.

Purpose: Deliver phase success criteria #4 (demo seed dataset) and #5 (all suites green) while closing the review's seed-idempotency, script-invocation, consent-marking, and fake-E2E gaps.
Output: `backend/seed_demo.py` that is safe to run repeatedly, machine-asserted seed correctness, a proven real-time loop, and a rehearsed demo.

## Review Findings Incorporated (08-REVIEWS.md)

| Finding | Disposition |
|---|---|
| HIGH — "render accurately" demo outcome exceeds `seed_demo.py` scope (Hospital/ASHA/Stakeholder workspaces are static, workspaces.tsx:308/400) | **Incorporated + documented** — the plan does NOT claim static pages render seed data. Seed-data correctness is verified by API inspection (GET /api/hospitals, /api/stakeholders, audit route) + test assertions; static workspaces remain presentation-only. |
| HIGH — "End-to-end demo flow succeeds" is not a verification method | **Incorporated** — Task 2 adds `TestVenomScoreHospitalLoop` (two-session: victim POST → backend broadcast recorded → venomType progression) + a manual two-browser rehearsal with a 15s timeout as the final human gate. |
| MEDIUM — script invocation contract (`from . import database` breaks as `python seed_demo.py`) | **Incorporated** — `sys.path.insert(0, os.path.join(ROOT, "backend"))` per setup.py:99. |
| MEDIUM — seed idempotency and stock history unclear | **Incorporated** — name-based upsert + delete-then-insert AntivenomStock per demo hospital + VillageAudit upsert by (gramPanchayat, district); idempotency machine-asserted. |
| MEDIUM — named stakeholder reputational risk | **Incorporated** — every seeded stakeholder uses `supportType 'pilot_permission'` (transparent demo marking; consent not verifiable in this repo). |
| LOW — seed correctness lacks tests | **Incorporated** — `test_seed_demo.py` asserts exact hospitals, compliance values, stock statuses, stakeholder names + supportType, and village aggregates against a temp DB. |
| Suggestion — split seed script from demo checklist / honest API inspection | **Incorporated** — Task 1 = seed + assertions; Task 2 = loop test + full gate + API inspection + human rehearsal. |

## Execution Context

@C:/Users/OM Prakash/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/OM Prakash/.config/opencode/gsd-core/templates/summary.md

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/app/seed.py
@backend/app/database.py
@backend/app/routes/venom_score.py
@backend/tests/test_routes.py
@setup.py

## Tasks

<task type="tracer">
  <name>Task 1: Idempotent Karnataka seed script + post-seed correctness & idempotency assertions</name>
  <files>backend/seed_demo.py, backend/tests/test_seed_demo.py</files>
  <read_first>
    - backend/app/seed.py — name-based upsert pattern (lines 28-52) and AntivenomStock insert
    - backend/app/database.py — Hospital/AntivenomStock/Stakeholder/VillageAudit schemas, migrate_db (ALTERs complianceScore onto Hospital), init_db, get_conn/new_id/now_iso
    - setup.py — sys.path.insert(0, BACKEND_DIR) contract (line 99)
  </read_first>
  <action>
    Create backend/seed_demo.py — standalone script (not a package module):
    - Module docstring "Seed realistic Karnataka demo data for the IISc presentation."
    - `ROOT = os.path.dirname(os.path.abspath(__file__))`; `sys.path.insert(0, os.path.join(ROOT, "backend"))`; `os.environ.setdefault("NAGRAKSHA_DB", "db/nagraksha.db")`; then `from app import database as db` and `from app.database import init_db, get_conn, new_id, now_iso`.
    - `init_db()` FIRST (its migrate_db() ALTERs complianceScore/complianceUpdatedAt/complianceRank onto Hospital — required before any compliance insert).
    - Hospitals (name-based upsert, exactly copying seed.py:31-47 shape): for each of [("Mandya District Hospital", 12.5213, 76.8948, "Mandya, Karnataka", "+918232220001", 91.5), ("Tumkur District Hospital", 13.3379, 77.1173, "Tumkur, Karnataka", "+918162202002", 78.0), ("Hassan District Hospital", 13.0057, 76.1005, "Hassan, Karnataka", "+918172268003", 56.0), ("K.R. Hospital Mysore", 12.2958, 76.6394, "Mysore, Karnataka", "+918212520004", 88.0), ("Rajarajeshwari Medical Nagara", 12.9141, 77.4986, "Bangalore, KA", "+918028605005", 45.0)] — SELECT id FROM Hospital WHERE name=?; UPDATE lat/lng/address/contact/active=1/complianceScore/complianceUpdatedAt/updatedAt when found, else INSERT with new_id(). Then AntivenomStock per hospital: DELETE FROM AntivenomStock WHERE hospitalId=? then INSERT one row (new_id(), hospitalId, "Indian Polyvalent Antivenom (VINS/Bharat Serums)", status = "CONFIRMED" if compliance > 70 else "LOW" if compliance > 45 else "OUT", "11-20 vials", now_iso(), "Dr. Pharmacy"). Print each seeded hospital + compliance.
    - Stakeholders (upsert by name; supportType ALWAYS 'pilot_permission' — transparent demo marking, not consent claims): Gerry Martin / The Liana Trust / Field Expert / Snake Rescuer / gerry@thelianatrust.org / Karnataka; Dr. Ravi Shankar / Mandya District Health Dept / District Health Officer / +919844001234 / Mandya; NSS Coordinator / MS Ramaiah Institute of Technology / Academic Pilot Partner / nss@msrit.edu / Bangalore.
    - VillageAudit (upsert by gramPanchayat + district): Malavalli/Mandya/12.3882/77.0827/4 households/58.0; Srirangapatna/Mandya/12.4278/76.7013/7/72.0; Tiptur/Tumkur/13.2641/76.4774/3/41.0 — ashaWorkerId "asha-worker-001", auditDate "2026-08-10", createdAt now_iso().
    - Final summary print with counts.

    Create backend/tests/test_seed_demo.py:
    - Set `os.environ["NAGRAKSHA_DB"]` to a tempfile path BEFORE importing app.database (mirror conftest.py lines 1-15 pattern); register atexit cleanup of the temp file.
    - Import the seed module and run its run() function twice in a row.
    - Assert: exactly the 5 demo hospital names present with exact complianceScore values (91.5/78.0/56.0/88.0/45.0); AntivenomStock status mapping (Mandya CONFIRMED, Hassan LOW, Rajarajeshwari OUT); exactly 1 AntivenomStock row per demo hospital AFTER the second run (idempotency — no accumulation); exactly 3 stakeholders with Gerry Martin present and every supportType == 'pilot_permission'; exactly 3 VillageAudit rows with Malavalli aggregateRiskScore == 58.0 and district Mandya; hospital count unchanged between run 1 and run 2.
  </action>
  <acceptance_criteria>
    - `cd backend && python seed_demo.py` exits 0 (print summary shows 5 hospitals / 3 stakeholders / 3 audits)
    - `cd backend && python -m pytest tests/test_seed_demo.py -v` exits 0 with all assertion tests passing
    - Second seed run produces identical row counts (no duplicate AntivenomStock rows) — asserted in-test
    - `grep -c "pilot_permission" backend/seed_demo.py` >= 3 (every stakeholder row)
  </acceptance_criteria>
  <verify>
    <automated>cd backend; python -m pytest tests/test_seed_demo.py -v; if ($?) { python seed_demo.py }</automated>
  </verify>
  <done>Karnataka demo dataset seeds idempotently and its correctness (names, compliance, stock mapping, aggregates, consent marking) is machine-asserted against a temp DB.</done>
  <reversibility rating="costly">Seeding real named people (Gerry Martin et al.) has reputational surface; mitigated by mandatory pilot_permission marking. Reruns are non-destructive to unrelated rows by construction.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Two-session hospital-loop test + full suite gate + API inspection + manual rehearsal</name>
  <files>backend/tests/test_routes.py</files>
  <read_first>
    - backend/tests/test_routes.py — TestVenomScore class shape (from 08-02) and pytestmark
    - backend/app/routes/venom_score.py — POST response + broadcast payload as built
  </read_first>
  <action>
    Add TestVenomScoreHospitalLoop to test_routes.py:
    - POST /api/sos to create an incident (json {"lat": 12.52, "lng": 76.89}); capture the incident id.
    - monkeypatch `app.routes.venom_score.broadcast` with an async fake that appends (event, payload) to a list.
    - Submit baseline reading (right_aperture 0.025, left_aperture 0.024, avg_aperture 0.0245, ptosis_detected false, severity "none", asymmetric false) → assert 200.
    - Submit ptosis reading (right_aperture 0.010, left_aperture 0.011, avg_aperture 0.0105, percent_change 57.1, ptosis_detected true, severity "moderate", asymmetric false, minutes_since_bite 18) → assert 200.
    - Assert the captured broadcast list has exactly 2 entries, both with event == "VENOM_SCORE_UPDATE"; first payload venomScore.venomType == "UNKNOWN", second == "NEUROTOXIC".
    - GET /api/venom-score/{id}/score → venomType "NEUROTOXIC" and estimatedAntivenomVials within the NEUROTOXIC band (15-25 given severity).

    Full suite gate (run and record): `cd backend && python -m pytest tests/ -q`; `cd frontend && npx vitest run`; `cd frontend && pnpm run lint`; `cd frontend && pnpm run build`.

    API inspection (honest verification per Review HIGH — static workspaces are presentation-only, seed correctness is proven through real routes): with the backend running and the seeded DB, verify GET /api/hospitals?lat=12.5&lng=76.9 returns the Karnataka hospitals with compliance-ranked order, GET /api/stakeholders returns Gerry Martin, and the village audit route returns Malavalli/Srirangapatna/Tiptur. Record the actual responses in the plan SUMMARY.

    Manual two-browser rehearsal (human gate, documented in SUMMARY): tab A (victim) triggers SOS → incident page → Start VenomScore → baseline → wait 10s → second reading → squint → closure % climbs, severity changes; tab B (hospital) opens /incidents/{id}?role=hospital → pre-arrival packet appears within 15s with updated vials. Any breakage is fixed in-place before the phase closes.
  </action>
  <acceptance_criteria>
    - TestVenomScoreHospitalLoop passes: 2 VENOM_SCORE_UPDATE events with UNKNOWN → NEUROTOXIC progression and in-band vial estimate
    - `cd backend && python -m pytest tests/ -q` exits 0; `cd frontend && npx vitest run` exits 0; `cd frontend && pnpm run lint` exits 0; `cd frontend && pnpm run build` exits 0
    - API inspection outputs recorded in the SUMMARY (hospitals ranked, stakeholders incl. Gerry Martin, village audits present)
    - Rehearsal checklist completed with no unresolved failures
  </acceptance_criteria>
  <verify>
    <automated>cd backend; python -m pytest tests/ -q; if ($?) { cd ../frontend; npx vitest run }; if ($?) { pnpm run lint }; if ($?) { pnpm run build }</automated>
  </verify>
  <done>The victim→backend→hospital real-time loop is machine-proven, every suite is green, seeded data is confirmed reachable through live API routes, and the two-browser demo rehearsal passed.</done>
  <reversibility rating="reversible">Test + verification artifacts only.</reversibility>
</task>

</tasks>

## Threat Model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script→DB | writes demo records into the local dev SQLite DB |
| victim POST→hospital WS | real-time score crosses from victim session to hospital session |
| demo DB→presentation | seeded records shown publicly during the IISc presentation |

### STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04-01 | Spoofing | Stakeholder rows (real person names) | medium | mitigate | every seeded stakeholder uses supportType 'pilot_permission' — transparent demo marking; no verified-consent claims (review MEDIUM) |
| T-08-04-02 | Integrity | repeated seed runs corrupt ranking inputs | medium | mitigate | name-based upsert + delete-then-insert AntivenomStock per demo hospital; idempotency machine-asserted (counts identical after 2nd run) |
| T-08-04-03 | Information Disclosure | demo DB medical-adjacent data | low | accept | backend/db/ is gitignored; seed writes only local dev DB or temp test DB; no PII beyond public-facing names/districts |
| T-08-04-SC | Tampering | npm/pip/cargo installs | N/A | N/A | no package installs in this plan |

## Verification

- `cd backend && python -m pytest tests/ -q` — full backend suite green (existing 61 + 08-02 venom tests + 08-04 loop + seed tests)
- `cd frontend && npx vitest run`, `cd frontend && pnpm run lint`, `cd frontend && pnpm run build` — all green
- `cd backend && python seed_demo.py` — exits 0, idempotent (run twice, same counts)
- API inspection outputs + rehearsal checklist recorded in 08-04-SUMMARY.md

## Success Criteria

1. `backend/seed_demo.py` seeds all required Karnataka data idempotently with exact compliance values, stock mapping, stakeholders (pilot_permission), and village audits.
2. The two-session real-time loop (victim POST → VENOM_SCORE_UPDATE → hospital-visible progression) is machine-proven.
3. Seeded data is verifiable through live API routes (not claimed through static UI).
4. All four automated gates pass; the manual two-browser rehearsal completed without unresolved failures.

## Artifacts This Phase Produces

- `backend/seed_demo.py` (NEW) — standalone script; `run()`; name-based upserts; AntivenomStock delete-then-insert; supportType 'pilot_permission' stakeholders; VillageAudit upserts; `sys.path.insert` + NAGRAKSHA_DB defaulting
- `backend/tests/test_seed_demo.py` (NEW) — temp-DB seed correctness + idempotency tests (hospitals/compliance/stock/stakeholders/audits)
- `backend/tests/test_routes.py` — modified: `TestVenomScoreHospitalLoop` (two VENOM_SCORE_UPDATE broadcasts, UNKNOWN→NEUROTOXIC progression)

## Output

Create `.planning/phases/08-venomscore-and-august-15-demo-execution/08-04-SUMMARY.md` when done