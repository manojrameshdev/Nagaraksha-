---
phase: 08-venomscore-and-august-15-demo-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - setup.py
  - .gitignore
  - frontend/package.json
  - frontend/store/sos-store.ts
  - backend/app/routes/sos.py
autonomous: true
requirements:
  - CLEANUP-01
  - BUGFIX-01
reference: "docs/AUGUST_15_EXECUTION_PLAN.md#hour-0--cleanup-first-both-people-30-minutes"
must_haves:
  truths:
    - "nag-raksha.zip is deleted from repository root"
    - "Prisma scripts (db:push, db:generate) removed from root package.json"
    - "setup.py runs pnpm install instead of npm install for frontend"
    - ".gitignore ignores /test instead of bare test"
    - "frontend/package.json name is nagraksha-frontend"
    - "sos-store.ts setIncident sets incidentId: incident.id so WebSocket deep links work"
    - "backend/app/routes/sos.py trigger_sos route has @limiter.limit('10/minute') rate limiting"
  artifacts:
    - path: "frontend/store/sos-store.ts"
      provides: "Zustand SOS store with fixed incidentId assignment"
      contains: "incidentId: incident.id"
    - path: "backend/app/routes/sos.py"
      provides: "Rate-limited SOS trigger endpoint"
      contains: "@limiter.limit(\"10/minute\")"
---

# Plan 08-01: Hour 0 Cleanup & Deep Link Bug Fix

## Objective

Complete the initial 30-minute pre-demo cleanup and fix the WebSocket deep linking bug in `sos-store.ts` and add rate-limiting to `sos.py`, as defined in [docs/AUGUST_15_EXECUTION_PLAN.md](file:///c:/Users/OM%20Prakash/Documents/Nagaraksha-/docs/AUGUST_15_EXECUTION_PLAN.md#hour-0--cleanup-first-both-people-30-minutes).

## Tasks

### Task 1: Clean root files & configs
- Delete `nag-raksha.zip`.
- Remove `"db:push"` and `"db:generate"` from root `package.json`.
- Update `setup.py` frontend install step from `["npm", "install"]` to `["pnpm", "install"]`.
- Update `.gitignore` bare `test` to `/test`.
- Update `frontend/package.json` name from `"my-project"` to `"nagraksha-frontend"`.

### Task 2: Fix WebSocket deep link bug
- In `frontend/store/sos-store.ts`, update `setIncident`:
```typescript
setIncident: (incident) =>
  set({
    incident,
    incidentId: incident.id,
    dispatchLanes: incident.dispatchAttempts ?? [],
  }),
```

### Task 3: Add rate limiting to SOS endpoint
- In `backend/app/routes/sos.py`, add `@limiter.limit("10/minute")` decorator to `trigger_sos`.

## Verification
- Backend tests: `cd backend && python -m pytest tests/ -v --tb=short -q`
- Frontend tests: `cd frontend && npx vitest run`
- Frontend lint: `cd frontend && pnpm run lint`
